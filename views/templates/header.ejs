<!doctype html>
<html lang="en">

<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>
    <%= title || "COMP4921 Project 3" %>
  </title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.0/font/bootstrap-icons.css">
  <link href="/css/style.css" rel="stylesheet">
  <style>
    #chat-bubble {
      position: fixed;
      bottom: 25px;
      right: 25px;
      width: 55px;
      height: 55px;
      background-color: #0d6efd;
      color: white;
      border-radius: 50%;
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 24px;
      cursor: pointer;
      box-shadow: 0px 4px 12px rgba(0, 0, 0, 0.25);
      z-index: 9999;
    }

    #chat-bubble:hover {
      background-color: #0b5ed7;
    }

    #chat-unread {
      position: absolute;
      top: -5px;
      right: -5px;
      background: red;
      color: white;
      font-size: 12px;
      padding: 4px 6px;
      border-radius: 50%;
    }

    #chat-window {
      position: fixed;
      bottom: 95px;
      right: 25px;
      width: 320px;
      max-height: 420px;
      background: #ffffff;
      border-radius: 12px;
      overflow: hidden;
      box-shadow: 0 4px 18px rgba(0, 0, 0, 0.25);
      z-index: 9999;
      display: flex;
      flex-direction: column;
    }

    #chat-window .chat-header {
      background: #0d6efd;
      color: #fff;
      padding: 8px 12px;
      display: flex;
      align-items: center;
      justify-content: space-between;
    }

    #chat-window .chat-body {
      padding: 10px;
      flex: 1;
      overflow-y: auto;
      background-color: #f8f9fa;
    }

    #chat-window .chat-footer {
      padding: 8px;
      display: flex;
      align-items: center;
      border-top: 1px solid #dee2e6;
      background-color: #ffffff;
    }

    .chat-message {
      margin-bottom: 6px;
      display: flex;
    }

    .chat-message.me {
      justify-content: flex-end;
    }

    .chat-message.them {
      justify-content: flex-start;
    }

    .chat-bubble-inner {
      max-width: 75%;
      padding: 6px 10px;
      border-radius: 12px;
      font-size: 0.85rem;
    }

    .chat-message.me .chat-bubble-inner {
      background-color: #0d6efd;
      color: #fff;
      border-bottom-right-radius: 2px;
    }

    .chat-message.them .chat-bubble-inner {
      background-color: #e9ecef;
      color: #212529;
      border-bottom-left-radius: 2px;
    }

    .chat-timestamp {
      display: block;
      font-size: 0.7rem;
      opacity: 0.7;
      margin-top: 2px;
    }
  </style>
</head>

<body class="d-flex flex-column min-vh-100">
  <script src="https://cdn.jsdelivr.net/npm/cloudinary-jquery@2.11.3/cloudinary-jquery.min.js"></script>
  <nav class="navbar navbar-expand-lg navbar-dark bg-dark position-relative">
    <div class="container">
      <a class="navbar-brand" href="/">Project3</a>
      <a class="navbar-brand" href="/friends">Friends</a>
      <div class="d-flex align-items-center">
        <% if (loggedIn) { %>
          <span class="me-2 text-white">
            Welcome, <strong class="text-warning">
              <%= username %>!
            </strong>
          </span>
          <a href="/activity" class="btn btn-outline-info btn-sm me-2 position-relative">
            <i class="fa-solid fa-bell"></i> Activity
            <span id="activity-badge"
              class="position-absolute top-0 start-100 translate-middle badge rounded-pill bg-danger d-none">
              0
            </span>
          </a>
          <a href="/profile" class="btn btn-outline-light btn-sm me-2">
            <i class="fa-solid fa-user"></i> Profile
          </a>
          <a href="/logout" class="btn btn-outline-danger btn-sm">Logout</a>
          <% } else { %>
            <a href="/login" class="btn btn-outline-primary btn-sm me-2">Login</a>
            <a href="/signup" class="btn btn-primary btn-sm">Sign Up</a>
            <% } %>
      </div>
    </div>
  </nav>
  <% if (loggedIn) { %>
    <div id="chat-bubble">
      <i class="fa-solid fa-comments"></i>
      <span id="chat-unread" class="badge d-none"></span>
    </div>
    <div id="chat-window" class="d-none">
      <div class="chat-header">
        <strong>Chat</strong>
        <div class="d-flex align-items-center gap-2">
          <select id="chat-friend-select" class="form-select form-select-sm">
            <option value="">Select a friend…</option>
          </select>
          <button type="button" id="chat-close" class="btn-close btn-close-white btn-sm"></button>
        </div>
      </div>
      <div class="chat-body" id="chat-messages">
        <div class="text-muted small">Select a friend to start chatting 👋</div>
      </div>
      <div class="chat-footer">
        <input id="chat-input" type="text" class="form-control form-control-sm" placeholder="Type a message..."
          disabled>
        <button id="chat-send" class="btn btn-primary btn-sm ms-2" disabled>
          <i class="fa-solid fa-paper-plane"></i>
        </button>
      </div>
    </div>
    <% } %>

      <script id="user-id-json" type="application/json">
        <%= JSON.stringify(currentUserId || null) %>
      </script>

      <script>
        const raw = document.getElementById('user-id-json').textContent.trim();
        window.CURRENT_USER_ID = raw ? JSON.parse(raw) : null;
        console.log("CURRENT_USER_ID =", window.CURRENT_USER_ID);
      </script>